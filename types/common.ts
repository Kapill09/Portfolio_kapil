export type Project = {
  id: string
  title: string
  description: string
}

export type Skill = {
  category: string
  items: string[]
}

export type Social = {
  name: string
  url: string
  icon: string
}
